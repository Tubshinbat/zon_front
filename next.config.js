const nextConfig = {
  // reactStrictMode: true,
  // strictMode: true,
  reactStrictMode: true,
  experimental: {
    appDir: true,
    scrollRestoration: true,
  },
};

module.exports = nextConfig;
